version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    # Read the Docs tokenises each job command, so quotes and `$( )` do not survive.
    post_install:
      - python -m pip install -r docs/requirements.txt

mkdocs:
  configuration: mkdocs.yml
